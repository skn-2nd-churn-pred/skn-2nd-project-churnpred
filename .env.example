# 환경변수 예시 파일.
# 실제 값은 .env 에 넣고 커밋하지 않는다 (.gitignore가 .env를 차단하고 있음).
#
# 이 프로젝트는 공개 CSV(Kaggle Cell2Cell)만 사용하므로 필수 키가 없다.
# Kaggle API로 데이터를 내려받을 경우에만 아래 값을 채우면 된다.

# KAGGLE_USERNAME=
# KAGGLE_KEY=
